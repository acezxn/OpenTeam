import { Button, List, ListItem, ListItemText, Typography } from "@mui/material"
import AnnouncementIcon from '@mui/icons-material/Announcement';

export const DiscussionBoard = () => {
    return (
        <div style={{ marginLeft: 10, marginRight: 10 }}>
            <Typography>To be implemented</Typography>
            {/* <Button variant="outlined" style={{position: "absolute", top: "calc(100vh - 50px)"}}>New discussion</Button>
            <List variant="outlined" style={{ maxHeight: "calc(100vh - 180px)", overflow: "auto", backgroundColor: "var(--board-dark-color)" }}>
                <ListItem style={{ padding: 0 }}>
                    <Button
                        color="inherit"
                        size="small"
                        fullWidth={true}
                        style={{ padding: 10, textTransform: "none", textAlign: "left" }}
                        onClick={() => { }}>
                        <ListItemText>
                            <AnnouncementIcon style={{ verticalAlign: "middle" }} />
                            <label style={{ padding: 10 }}>test title</label>
                        </ListItemText>
                    </Button>
                </ListItem>
            </List> */}
        </div>
    )
}